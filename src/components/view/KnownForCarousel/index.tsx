import React from "react"
import { CastCreditsProps } from "~/types/data/combinedCredits"

const KnownFor = ({ data }: { data: CastCreditsProps[] }) => {
    return <div>KnownFor</div>
}

export default KnownFor
